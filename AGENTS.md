# AGENTS.md

## Cursor Cloud specific instructions

This repository (`MelonScount-BE`) is described as "Backend of MelonScount." As of initial setup, it is a greenfield project containing only `README.md` and `LICENSE` (MIT, Copyright 2026 Jiawei-Tang). There is no application code, build system, or dependency manifest yet.

- **No services to start.** There are no backend processes, databases, or infrastructure dependencies.
- **No lint/test/build commands available.** No `package.json`, `requirements.txt`, `Makefile`, `Dockerfile`, or similar files exist.
- Once application code is added, update this section with service startup instructions, lint/test/build commands, and any non-obvious caveats discovered during development.
